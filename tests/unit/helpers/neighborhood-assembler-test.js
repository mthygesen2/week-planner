import { neighborhoodAssembler } from 'weekend-planner/helpers/neighborhood-assembler';
import { module, test } from 'qunit';

module('Unit | Helper | neighborhood assembler');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = neighborhoodAssembler([42]);
  assert.ok(result);
});
