import { imageSize } from 'weekend-planner/helpers/image-size';
import { module, test } from 'qunit';

module('Unit | Helper | image size');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = imageSize([42]);
  assert.ok(result);
});
